export class DisplayLevelObject {
    // Purely for displaying of objects, converts from data size of 1 to 32px e.g.
    width: number;
    height: number;
    x: number;
    y: number;
    type: String;
    id: String;
}
